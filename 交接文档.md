# LX Music 修改版 — 项目交接文档

> 写给接手的人。读完这一份，你应该能搞清楚这个项目改了什么、怎么继续开发、怎么打包发布。

## 一、这是什么

基于开源项目 **LX Music（洛雪音乐）移动版 v1.9.1**（`lyswhut/lx-music-mobile`，commit `fb84807`）源码构建的**第三方修改版**，React Native 0.73 + TypeScript + React Native Navigation，只支持 Android（targetSdk 29）。

仓库：`https://github.com/azx8788a/lx-music-mobile`（公开，是上游的 fork，保留了完整共同提交历史，master 领先上游若干自有提交）。

与原版的差异（按实现时间顺序）：

1. **内置 23 个第三方音源**：开箱即用，首启自动导入
2. **音乐下载功能**：选源选质、串行队列、下载管理
3. **修改版公告**：仿原版许可协议风格，首启展示，10 秒倒计时同意
4. **网易云收藏歌单导入**：填一次 MUSIC_U Token，列出账号全部歌单

## 二、资源与凭据

| 东西 | 位置 | 说明 |
|---|---|---|
| 源码工作区 | `/workspace/lx-music-mobile` | 即本仓库 master，node_modules 已装好 |
| APK 签名密钥 | `/workspace/_keystore/net.lvtao.tool.keystore` | 别名 `net.lvtao.tool`，密码见同目录 `PASSWORD.txt`，**不要提交进仓库/写进文档** |
| GitHub 账号 | `azx8788a` | 访问令牌向仓库主人索取（登录 GitHub → Settings → Developer settings → PAT 可随时生成/吊销） |
| Actions secrets | 仓库 Settings → Secrets | 已配好 5 个 `KEYSTORE_*`，指向上面的密钥 |

## 三、功能实现速查（改了哪些文件）

### 1. 内置音源
- `src/config/builtinUserApi/`：23 个音源脚本转成的 TS 字符串模块（`source_1.ts` ~ `source_23.ts` + `index.ts`），**由 `V260917.zip` 生成，更新音源时用 python 重新生成，文件头部带 `/* eslint-disable */`**
- `src/core/init/userApi/builtin.ts`：首启导入逻辑。用 `storageDataPrefix.userApiBuiltinImported` 标记防止重复导入；导入后关闭音源更新弹窗（`allowShowUpdateAlert=false`，避免首启弹窗轰炸）
- `src/core/init/index.ts`：`common.apiSource` 为空时默认选第一个导入成功的聚合音源（HYWmusic）
- `src/core/userApi.ts`：`importUserApi` 改为返回 info

### 2. 下载功能
- `src/core/download/index.ts`：核心。串行队列（公益音源普遍限流，**不要加并发**）、`getMusicUrl` 取链接（失败自动换源）、RNFS 下载到 `保存路径/文件名.dlpart` 再 moveFile、暂停/继续/重试/删除、任务列表持久化（`storageDataPrefix.downloadTaskList`）、重启恢复
- **2026-09-25 下载附加信息（v1.9.8）**：下载完成后自动写入歌曲标签（歌名/歌手/专辑）、封面，并保存歌词（内嵌到标签 + 同名 `.lrc` 文件），核心在 `src/core/download/metadata.ts`（`ape` 不支持标签，仅写 .lrc；封面先下载到临时目录再 `writePic`）；开关 `download.isWriteMetadata` / `download.isSaveLyric`（默认开，设置 → 基础设置 →「下载附加信息」）；附加信息失败**不阻塞**下载，任务仍标 completed，toast 提示 + `[下载]` 日志
- **2026-09-20 修复（详见根目录「下载功能排查报告.md」）**：
  - ★ 修复单曲下载静默失败：普通模式（未多选）下 `selectedList` 为空导致两个 ListMenu 的下载分支什么都不做，现改为「为空时下载当前歌曲」（`OnlineList/ListMenu.tsx`、`Mylist/MusicList/ListMenu.tsx`）
  - ★ 修复非 2xx 被当成功：RNFS 对 HTTP 错误码不抛错而是正常 resolve，现检查 `statusCode`（非 2xx → DL_HTTP_001）与 `bytesWritten`（空文件 → DL_NET_001），并校验 moveFile 后文件存在
  - 错误码体系：`DL_URL_001` 获取链接失败 / `DL_HTTP_001` HTTP 非2xx / `DL_NET_001` 网络或空文件 / `DL_FS_001` 目录不可用（含权限申请重试）/ `DL_FS_002` 写入失败 / `DL_FS_003` 移动失败 / `DL_TASK_001` 其他；任务新增 `errorCode`/`errorPhase` 字段（旧数据兼容）
  - 全流程日志：前缀 `[下载]`（创建/开始/链接域名/目录/HTTP 状态/完成/失败/暂停/重试/移除/启动恢复），不记录完整 URL
  - 提示：创建任务「正在发起下载…」/ 批量「已发起 {num} 个下载任务」/ 完成「下载完成」/ 失败「下载失败 [{code}]」
  - 下载管理弹窗显示错误码（`error [DL_XXX]: message`）
- **2026-09-23 存储授权修复（v1.9.6）**：
  - 新增 `src/utils/storagePermission.ts`：权限三态化 `granted / denied / blocked`（blocked=勾选"不再询问"永久拒绝）；`showStoragePermissionDialog()` 授权窗口：denied→应用内说明后重试；blocked→`Linking.openSettings()` 跳系统设置 + AppState 回前台自动复查
  - 下载目录准备改为「mkdir 容忍 EEXIST + 写入探针验证」（`tryPrepareSaveDir`）：修复"已授权仍报 DL_FS_001"（exists 在无权限时返回 false → 对已存在目录 mkdir 报 EEXIST 被误判为权限失败）
  - 失败时自动收集存储诊断（系统版本/品牌型号/权限状态）写入错误日志，排查国产 ROM 二级权限墙
  - 设置页下载路径处新增「检查存储授权」入口
  - 任务状态新增「等待存储授权」（download_status_wait_permission）
  - **CI 存储检查实验**：`.github/workflows/storage-check.yml`（手动触发），在 API 30/33 模拟器上核对：pm grant WRITE 是否成功、LEGACY_STORAGE appop 状态——验证 target 29 + legacy 在 Android 11+ 是否仍可用（专家判定：分界线是 targetSdk>=30，target 29 全版本可用）
  - ⚠️ 待办：真机验证（Android 10 + Android 13 各一台）；国产 ROM（MIUI/HarmonyOS）二级权限墙需真机排查
- `src/store/download/`：state/action/event/hook（照 `store/userApi` 的模式写的）
- `src/navigation/components/DownloadModal.tsx`：选源选质 overlay（`getOtherSource` 自动找其他平台版本）
- `src/navigation/components/DownloadManagerModal.tsx`：下载管理 overlay（进度/速度/操作）
- 入口：歌曲菜单「下载」（`components/OnlineList/ListMenu.tsx`、`Mylist/MusicList/ListMenu.tsx`，多选=按默认音质批量）；下载管理图标在「我的列表」页头部（`ActiveList.tsx`）
- 设置：`download.savePath`（空=外置存储/Music）、`download.quality`、`download.fileName`，UI 在 `Setting/settings/Basic/Download*.tsx`

### 3. 修改版公告
- `src/navigation/components/ModNoticeModal.tsx`：内容=我们是什么+改了什么+免责+免费声明；10 秒倒计时才可点「同意并进入」，不同意 exitApp
- `src/navigation/components/PactModal.tsx`：原版协议首签后接续弹修改版公告（`showModNoticeModal()`）
- `src/core/init/index.ts`：从原版升级的老用户（已签协议但没看过修改版公告）首启也弹
- 设置项 `common.isAgreeModNotice`

### 4. 网易云收藏歌单导入（实验性功能，未经真机验证）
- **登录方式一：内置浏览器登录**（推荐，`src/navigation/components/WyLoginModal.tsx`）
  - WebView 打开 `https://music.163.com/m/login`，用户在页面中自行完成登录（手机号验证码/密码均可）
  - 新增原生模块 `android/.../cookie/CookieModule.java`（+ `CookiePackage`，已在 MainApplication 注册）与 JS 封装 `src/utils/nativeModules/cookie.ts`：读取 WebView CookieManager——MUSIC_U 是 HttpOnly Cookie，`document.cookie` 读不到，必须原生取
  - 每 1.5s 轮询 Cookie，发现 MUSIC_U 后先请求登录态接口验证（失效 Cookie 不保存，仅网络错误会继续重试），有效则保存 `wy.musicUToken` + `wy.userInfo`（JSON：uid/nickname/avatarUrl），并 flush 落盘 WebView Cookie
  - 新依赖：`react-native-webview`（^13.17.0）
- **登录方式二：扫码登录**（界面已标记「不稳定」，作为备选，`src/navigation/components/WyQrLoginModal.tsx`）
  - `src/utils/musicSdk/wy/login.js`：`getQrKey`（`/weapi/login/qrcode/unikey`）+ `getQrUrl`（`https://music.163.com/login?codekey=<unikey>`）+ `checkQrStatus`（`/weapi/login/qrcode/client/login`，801 等待 / 802 已扫码显示头像昵称 / 803 成功时从响应 Set-Cookie 提取 MUSIC_U / 800 过期）
  - **实测二维码约 20 秒即过期**（远低于预期的分钟级），故标记为不稳定；弹窗内有提示文案，2s 轮询 + unikey 守卫防旧二维码延迟响应污染
  - 二维码渲染：内置纯 JS 库 `src/utils/qrcode.js`（vendored qrcode-generator@1.4.4，MIT；配套 `qrcode.d.ts`；按行合并连续黑块渲染减少 View 数量），无原生依赖
- **登录态接口已修正**：`getLoginStatus` 原用 `/weapi/login/status`（**已失效返回 404**），改为 `/weapi/w/nuser/account/get`；Token 无效时抛出带 `code = 'INVALID_TOKEN'` 的错误供调用方区分网络错误
- 歌单列表接口：`/weapi/user/playlist`（不变，`wy/userPlaylist.js`，带 MUSIC_U Cookie，失败重试 2 次）
- 设置页 UI：`Setting/settings/Basic/WyMusicUToken.tsx`（标题「网易云账号（实验性）」；登录状态显示昵称 / 网页登录 / 扫码登录（不稳定）/ 退出登录 / 手动填写 Token 兜底）
- **侧边栏「网易云歌单」页**（`src/screens/Home/Views/WySonglist/index.tsx`，v1.9.7 快照版）：左侧菜单「我的列表」与「设置」之间新增入口（`NAV_MENUS` 中 `nav_wy_songlist`，无图标项，纯文字；竖向 `DrawerNav` 与横向 `Aside` 均已适配 icon 为 null 的情况）；页面**本地快照优先展示**（离线可用，token 失效也能看），工具栏显示「上次更新」时间 + 更新/导出/导入按钮；点击歌单进入 SonglistDetail；未登录/失效时提供两种登录入口
- **★ 网易云歌单快照**（v1.9.7，`src/core/wySnapshot.ts`）：
  - 登录成功后自动**全量保存**账号所有歌单及歌曲到本地（`@wy_snapshot_meta` + `@wy_snapshot_songs__<id>`；串行 800ms 间隔防限流；单个失败保留旧数据不中断）
  - **自动更新**：`wy.snapshotUpdateInterval` 设置项（12/24/48 小时 / 关闭，设置 → 基础设置），进入歌单页或 App 启动时检查过期自动更新
  - **导出/导入**：`.lxmc` 文件（含全部歌单+歌曲）；导入智能合并——新歌单自动添加、完全相同跳过、有缺失弹窗询问是否移除（选「保留」则只增不删）
  - 退出登录后快照保留，可离线查看；日志前缀 `[WY 快照]`
- 歌单列表：`src/navigation/components/WyUserPlaylistModal.tsx`，未登录时提供两种登录入口，登录状态变化自动重新加载（`useSettingValue` 订阅）；点击歌单进入 SonglistDetail（详情页有现成的「收藏歌单」按钮）
- **日志**：登录与歌单获取全过程写入错误日志（`@/utils/log` 的 `log.info/warn/error`），前缀 `[WY 网页登录]` / `[WY 扫码登录]` / `[WY 歌单]` / `[WY 歌单页]` / `[WY]`，设置 → 其他设置 → 错误日志 → 查看日志可排查；**MUSIC_U 不落日志**（只记录长度）
- 入口一（主）：侧边栏「网易云歌单」页
- 入口二：歌单页 →「打开歌单」弹窗内「从我的网易云歌单选择」（`OpenList/Modal.tsx`，未改动）
- i18n：`nav_wy_songlist`、`setting_basic_wy_musicu_*`、`wy_login_*`、`wy_web_login_*`、`wy_qr_login_*`、`wy_playlist_*` 三语已加
- 附：所有接口路径均在沙箱用 Python（与 `wy/utils/crypto.js` 同算法的 weapi 加密）对真实接口实测确认

## 四、构建与发布

```bash
# 本地校验（提交前必做）
npx tsc --noEmit     # 允许剩 4 个存量错误，见下节
npx eslint <你改的文件>

git add -A && git commit -m "..." && git push origin master
```

**push 不会自动触发构建**（fork 仓库上 on.push 失效，原因未知）。发布要手动触发，两种方式：

- 网页：仓库 Actions 页 → Build → Run workflow → master
- API：
```bash
curl -X POST -H "Authorization: token <你的令牌>" \
  https://api.github.com/repos/azx8788a/lx-music-mobile/actions/workflows/release.yml/dispatches \
  -d '{"ref":"master"}'
```

产物 10~20 分钟后出现在：Release 页 `releases/tag/v<package.json版本号>`（5 个 APK：arm64-v8a / armeabi-v7a / x86_64 / x86 / universal）+ run 页 Artifacts。

## 五、规范与坑（重要）

1. **tsc 存量错误**：`timeoutExit.ts`、`plugins/player/hook.ts`、`SongList/HeaderBar/SourceSelector.tsx`、`store/player/action.ts` 共 7 个错误是上游遗留，**与本项目改动无关，不要顺手去修**（会和上游合并冲突）。判断标准：改完代码后 `npx tsc --noEmit` 输出的错误文件若只含这 4 个，即视为通过。
2. **eslint 必须过**：standard-with-typescript 规则很严（eol-last、no-floating-promises 需要 `void fn()`、箭头函数不能直接返回 void 表达式等），提交前对改动文件跑一遍。
3. **i18n 三语**：新增用户可见文案必须同时加 `src/lang/zh-cn.json`、`zh-tw.json`、`en-us.json`，缺了 tsc 直接报错（key 联合类型校验）。
4. **全局弹窗用 RNN overlay**：照抄 `navigation/utils.ts` 里的 `show*Modal` 模板（screenNames + registerScreens + utils 三处 + 组件接收 `componentId`，`Navigation.dismissOverlay(componentId)` 关闭），带 300ms 防抖。跨屏可用，不要在具体 screen 里挂组件。
5. **KEYSTORE_STORE_FILE secret 的值必须是纯文件名**（`net.lvtao.tool.keystore`），工作流模板自带 `app/` 前缀，带路径必失败。
6. fork 仓库 GITHUB_TOKEN 默认只读，已把仓库 Actions workflow permissions 改为 write（Settings → Actions → General → Workflow permissions），Release job 才能发版。若以后重建 fork 需重新设置。
7. **同步上游**：fork 有共同历史，可 `git remote add upstream https://github.com/lyswhut/lx-music-mobile.git && git fetch upstream && git merge upstream/master`，冲突优先保我们的功能文件。
8. RN 相关：改完 JS 一般不用重装依赖；`npm ci` 只在 package-lock 变化后跑。

## 六、接手后建议按此顺序做

1. **真机验证网易云功能**（实验性，两种登录方式均未上过真机）：
   - 设置 → 基础设置 → 网易云账号（实验性）→「网页登录」→ 在内置浏览器页面中完成登录 → 应提示「登录成功」并自动关闭，设置页显示昵称
   - 备选：「扫码登录（不稳定）」→ 网易云音乐 App 扫码确认（二维码约 20 秒过期，需及时扫；过期点刷新）
   - 歌单页 → 打开歌单 → 「从我的网易云歌单选择」→ 应直接列出账号歌单（登录状态已保存，下次打开即可直接查看）→ 点进详情页 →「收藏歌单」
   - 手动 Token 兜底入口仍保留（设置页「手动填写 Token」），可对照排查是登录问题还是接口问题
   - **排查先看错误日志**：设置 → 其他设置 → 错误日志 → 查看日志，登录/歌单事件均有 `[WY 网页登录]` / `[WY 扫码登录]` / `[WY 歌单]` 前缀记录
   - 若接口失效：参考 NeteaseCloudMusicApi 项目的 `login_qr_key.js` / `login_qr_check.js` / `login_status.js` / `user_playlist.js` 更新 `src/utils/musicSdk/wy/` 下对应模块的路径/参数（注意 login/status 已改 `w/nuser/account/get`）
2. 触发一次构建，装机回归：首启公告弹窗（协议→修改版公告 10s 倒计时）、内置音源自动导入并默认选中、播放/下载、下载管理。
3. 可选的后续方向（按价值排序）：
   - 音源更新机制：现在音源打包死在 `builtinUserApi/`，可以做「检查 zip 更新并重新导入」
   - 下载列表的应用内播放入口（`LX.Download.ListItem` 的本地播放路径，桌面版有现成实现可抄）
   - 酷狗/QQ 音乐的同类 Token 歌单导入（kg 有 `uid###token` 格式，qq 有 xmusic_u Cookie，参考 wy 的实现即可）

## 七、环境备注

- 开发沙箱：Ubuntu 24.04 aarch64 proot，Node v20；**后台进程会被杀，长任务前台跑**
- GitHub 走用户的全局代理（美国节点时直连最快；国内节点时 ghfast.top 镜像更快——git config 里按需增删 `url.insteadOf`）
- 音源原始包 `/workspace/V260917.zip`；生成脚本要点：python 读取 zip → `json.dumps` 转义 → 每个音源一个 `source_N.ts`，文件名按字母序对应 `source_1..23`
