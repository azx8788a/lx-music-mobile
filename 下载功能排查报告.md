# LX Music 修改版 — 下载功能排查报告（最终版）

> 生成日期：2026-09-20（修复前排查完成，修复方案已同步实施）
> 范围：下载入口、下载队列、下载管理、文件写入、错误提示与日志。

## 一、用户现象

1. 点击歌曲菜单「下载」后基本没有反馈（无提示、无弹窗、无任务）。
2. 「我的列表 → 下载管理」中看不到任何任务。
3. 期望：正在发起下载 / 下载中 / 下载完成 / 下载失败 + 错误码；全过程写入错误日志。

## 二、确凿根因（排查结论）

### ★ 根因 A：单曲下载静默失败 —— 这就是“点击下载没动静、管理列表为空”的直接原因

**触发条件**：普通模式（非多选）下，点歌曲右侧「⋮」菜单 →「下载」。

**链路分析**：

- 列表菜单弹出时传入 `selectedList: listRef.current!.getSelectedList()`（`OnlineList/index.tsx:70`、`Mylist/MusicList/index.tsx:70`）；
- **普通模式从未进入多选，`selectedList` 恒为空数组**（`selectedListRef` 初始 `[]`，只有多选勾选时才有值）；
- 于是两个菜单组件的 download 分支全部落空：

```ts
// src/components/OnlineList/ListMenu.tsx（修复前）
case 'download': {
  if (selectInfo.selectedList.length == 1) {   // 普通模式下为 0，永远不成立
    selectAndDownload(selectInfo.musicInfo)
  } else {
    void createDownloadTasks(selectInfo.selectedList)  // 传入空数组 → 循环 0 次 → 静默无操作
  }
  break
}

// src/screens/Home/Views/Mylist/MusicList/ListMenu.tsx（修复前）
case 'download': {
  const onlineList = selectInfo.selectedList.filter(i => i.source != 'local')
  if (!onlineList.length) return                   // 空数组 → 直接 return → 静默无操作
  ...
}
```

**结论**：普通模式点「下载」= 什么都不发生（没有任务、没有弹窗、没有 Toast），加入下载管理自然也是空的。
只有「多选模式恰好选中 1 首」时才会弹出下载弹窗，「多选 2 首以上」才会批量下载——普通路径完全断掉。

### ★ 根因 B：HTTP 非 2xx 被当成下载成功

`react-native-fs` 的 `Downloader.java`：非 2xx 状态码时**不下载文件、不抛异常**，而是以 `statusCode` 字段正常 resolve。当前代码仅 `await promise`，未检查：

```ts
await promise                                    // 404/403 也“成功”
await moveFile(tmpPath, task.metadata.filePath).catch(() => {})  // 静默失败（文件不存在）
task.isComplate = true
task.status = 'completed'                        // ← 假“完成”
toast(global.i18n.t('download_success'))         // ← 假“下载完成”
```

### ★ 根因 C：文件系统错误全部被吞掉

- `await mkdir(getSaveDir()).catch(() => {})`：目录创建失败（含存储权限未授予）无感知；
- `await moveFile(...).catch(() => {})`：移动失败无感知，且仍标记 completed；
- 无任何持久化日志，问题不可追溯。

### 其他

- 存储权限：下载流程从不申请 `WRITE_EXTERNAL_STORAGE` 运行时权限（只在选文件夹组件里申请），默认路径 `/storage/emulated/0/Music` 在新装设备上可能直接不可写。
- 反馈过少：仅在完成时有一次 Toast，开始/失败均无提示。

## 三、修复方案（已实施）

### 1. 修复下载入口（根因 A）

普通模式下将 `selectedList` 为空视为“下载当前歌曲”：

```ts
const list = selectInfo.selectedList.length ? selectInfo.selectedList : [selectInfo.musicInfo]
if (list.length == 1) selectAndDownload(list[0]) else void createDownloadTasks(list)
```

同时 `Mylist` 版过滤本地歌曲后按同样规则处理。

### 2. 严格校验下载结果（根因 B）

- 检查 `result.statusCode`，非 2xx 抛错 → `DL_HTTP_001`；
- `result.bytesWritten <= 0` 视为空文件 → `DL_NET_001`；
- `moveFile` 后检查目标文件存在，不存在 → `DL_FS_003`；
- 只有真正落盘成功才置 `completed`。

### 3. 目录与权限（根因 C）

- 下载前：目录不存在则创建，仍不可用则**申请存储权限**后重试一次；
- 仍失败 → `DL_FS_001`，不再静默。

### 4. 错误码表（写入任务字段与日志）

| 错误码 | 含义 |
|---|---|
| `DL_URL_001` | 获取音乐链接失败（含换源失败） |
| `DL_HTTP_001` | 下载响应 HTTP 状态码非 2xx |
| `DL_NET_001` | 下载请求失败（网络中断/空文件等） |
| `DL_FS_001` | 下载目录不可用（创建失败或存储权限未授予） |
| `DL_FS_002` | 下载文件写入失败（如 ENOENT） |
| `DL_FS_003` | 文件移动/落盘失败 |
| `DL_TASK_001` | 其他任务异常 |

任务新增可选字段 `errorCode` / `errorPhase`（兼容旧数据）。

### 5. 提示与日志

- Toast：创建任务「正在发起下载…」（单曲）/「已发起 {num} 个下载任务」（批量）；完成「下载完成」；失败「下载失败 [{code}]」；
- 日志（前缀 `[下载]`，写入错误日志，设置页可查）：创建任务、目录就绪、获取链接（仅记录链接域名，不记录完整 URL）、HTTP 状态与文件大小、完成落盘、失败（错误码+阶段+原因）、暂停/继续/重试/移除、启动恢复。

## 四、修复后验证清单

1. [ ] 单曲下载：点击后有「正在发起下载…」，弹窗确认后管理列表可见任务；
2. [ ] 状态流转 `等待中 → 获取链接中 → 下载中 → 已完成`；
3. [ ] 失败场景有错误码（如断网 `DL_NET_001`、路径问题 `DL_FS_001`）；
4. [ ] 非 2xx 响应不会标记完成；
5. [ ] 错误日志含完整过程（`[下载]` 前缀）；
6. [ ] 批量下载串行执行，无并发；
7. [ ] 重启 App 后可恢复未完成任务；
8. [ ] tsc 仅剩 4 个已知存量错误文件；eslint 通过。